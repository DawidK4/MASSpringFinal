spring.application.name=MAS

# MySQL Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/mas?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=student
spring.datasource.password=Dawid@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Optional: Use the following to disable automatic schema generation
# spring.jpa.hibernate.ddl-auto=none
# spring.jpa.generate-ddl=false